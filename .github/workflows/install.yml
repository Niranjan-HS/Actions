name: install Workflow

on:
  push:

jobs:
  install:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Run app
        run: npm start
